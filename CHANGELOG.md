# 更新日志

本项目遵循语义化版本号。公开版本之前的开发提交已整理为首个发布快照。

## 0.8.0 - 2026-08-30

### 变更

- 手动及补偿重试任务与字幕包缓存命中任务使用相同的高优先级队列，避免全库批处理时重试长期排在队尾。
- 启用对轴时先做字幕结构校验，再把片源时长边界留到对轴后验证，允许 ffsubsync 修复不同版本的时间轴；未对轴兜底仍必须通过时长校验。
- 对轴前通过 ffprobe 过滤不可解码、解说或辅助音轨，并在语音检测失败时有限尝试后续音轨；同时把实际音轨和失败诊断写入任务事件。
- 电影和剧集的标题、年份、季集号及外部 ID 统一以本地 NFO 为唯一身份来源；
  MoviePilot 通知与 Jellyfin 手动任务在路径解析后进入同一条处理链路。
- Jellyfin 只用于媒体库浏览、海报、目录选择、字幕状态和下载后的刷新，不再参与
  字幕搜索身份补全。
- MoviePilot 回调早于 NFO 写入时，任务最多等待 120 秒；电影优先等待权威
  `movie.nfo`，避免读取仍在生成或内容错误的同名 NFO。
- 剧集以 `tvshow.nfo` 确认作品身份，并以有效分集 NFO 或文件名 `SxxExx` 确认季集号；
  错误写成 `<movie>` 的分集 NFO 不再污染剧集身份。

### 测试

- 增加 NFO 安全解析、冲突检测、延迟写入和两条入口一致性测试。
- 困难匹配数据集改为先构造电影与剧集 NFO，再验证年份防串片、数字标题、同名作品和
  季集匹配。

## 0.7.2 - 2026-08-03

### 修复

- 匹配整季包获得 42 分季集适配加分，精确匹配单集保持 40 分；同等质量下优先
  整季包，并统一适用于所有字幕源 Adapter。

## 0.7.1 - 2026-08-03

### 修复

- Zimuku 电影检索在“标题 + 年份”无结果时，会继续尝试中文标题和原始标题。
- 支持校验和识别带 BOM 的 UTF-16、UTF-32 文本字幕。
- RAR 字幕包在 7-Zip 不支持当前压缩格式时，使用 `lsar` 与 `unar` 列表和解压。
- 修复 Zimuku 仅标记为“中英双语/双语”的候选被误判为非中文字幕。
- 修复把上传日期误读为下载量的问题；同等匹配条件下优先尝试下载量较高的候选。
- 修复下载响应文件名包含 HTML 实体时扩展名被截断的问题。
- ASSRT 将 `langdou=true` 识别为中文双语，避免遗漏站点未明确标注中文的双语候选。
- Zimuku 剧集检索优先使用“第二季”“第二季第三集”等中文表达，并保留英文与
  `SxxExx` 回退。
- 剧集年份校验同时接受父级剧集首播年与当季、单集首播年。
- SubDL 搜索日志显示实际 IMDb ID、标题及每次查询结果，不再只显示内部计数键。

### 变更

- 候选接口增加可选的 Adapter 原生质量分；候选仍按来源逐个处理，不跨字幕源比较。
- Zimuku 和 ASSRT 在同源候选内优先参考下载量，再参考站点评分；双语仅在质量接近时
  优先，避免冷门双语字幕压过更可靠的高下载量字幕。
- ASSRT 搜索会读取候选详情统计并短时缓存。受官方 API 限速影响，候选较多时耗时会
  增加。
- 移除已经停止向普通用户提供服务的 OpenSubtitles.org XML-RPC 来源；Subliminal
  仅保留需要账户认证的 OpenSubtitles.com。

### 文档

- 增加字幕源搜索策略、字幕校验与对轴流程说明。
- 更新路线图、Zimuku 文档和 README 参考项目列表。

## 0.7.0 - 2026-08-01

### 新增

- 独立持久化的系统日志，记录服务启停、队列恢复、健康检查、配置变更和任务结果。
- 系统日志支持按类别、级别和任务筛选，并通过 SSE 实时更新。

### 变更

- 任务搜索、下载、校验和对轴细节只在任务工作台展示，系统日志仅保留任务开始与结果。
- 精简 README，保留快速部署、首次配置、MoviePilot 对接和更新备份等必要内容。

## 0.6.1 - 2026-07-30

### 新增

- 系统健康检查弹窗，逐项显示检查进度、结果和错误原因。
- Compose 为拾幕与 MoviePilot OCR 配置独立 bridge 网络，并暴露 OCR 诊断端口。

### 变更

- 重新设计系统健康页面，按运行环境、连接、工具和队列分类展示。
- Jellyfin 用户身份改为通过 API Key 自动发现，WebUI 不再要求填写 User ID。
- 快速部署只需修改拾幕主目录和媒体库目录，优先通过 Compose 对齐媒体路径。

## 0.6.0 - 2026-07-29

### 新增

- 单一 `/appdata` 目录部署，首次启动自动生成配置、数据与缓存目录。
- 首次使用向导、顶部配置进度、动态通知和系统健康检查。
- MoviePilot 首次成功回调验证、Jellyfin 实际连接测试和 Zimuku OCR 状态记录。
- WebUI 运行配置导入与导出。
- 可直接粘贴的 Compose、README Logo 与界面预览入口。

### 变更

- 移除首次部署对 `.env` 和手工创建目录、配置文件的依赖。
- Jellyfin 海报和整季字幕包缓存统一写入 `/appdata/cache`。
- 侧边栏改用功能图标并提高文字可读性。

## 0.5.0 - 2026-07-26

首个公开预览版本。

### 新增

- MoviePilot ChineseSubFinder 兼容回调与可选 Bearer Token。
- Jellyfin 媒体库浏览、海报缓存、字幕状态扫描和批量任务创建。
- Subliminal、ASSRT、SubDL、Zimuku Provider。
- 外挂与内嵌中文字幕检查、候选排序、字幕校验、ffsubsync 保守对轴。
- 整季包缓存、候选去重、失败候选排除和 Provider 频率调度。
- 任务工作台、实时日志、诊断、设置和 Provider 优先级管理。
- 拾幕 WebUI、Docker 部署文件与本地 MoviePilot OCR 服务。
