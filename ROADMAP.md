# 路线图

拾幕当前以“能稳定完成 MoviePilot 通知和 Jellyfin 手动补字幕”为目标，不扩展
成完整的 arr/Bazarr 替代品。

## 已完成

- 单一 `/appdata` 挂载、配置自动生成和可直接粘贴的 Compose。
- MoviePilot 回调、Jellyfin 媒体库浏览、批量任务、忽略状态和字幕状态扫描。
- Subliminal、ASSRT、SubDL、Zimuku 四个字幕源及可调整的搜索顺序。
- 外挂与内嵌字幕检查、整季包复用、候选校验、保守对轴和失败候选回退。
- 首次设置向导、配置进度、路径映射、系统健康检查和配置导入导出。
- 任务工作台、系统日志、SSE 更新、诊断信息和中文 WebUI。
- GitHub 自动测试以及 `amd64`、`arm64` 镜像发布流程。
- Adapter 原生质量排序；Zimuku 和 ASSRT 使用站内下载量与评分，双语仅在质量接近时
  优先。

## 下一步

- 在任务详情中手动指定某个候选，执行下载、校验和落库。
- 在下一次修改匹配或校验规则前，建立小型离线回归样本，覆盖年份错误、
  电影与剧集串线、错误季集、整季字幕包、空文件、HTML 错误页、压缩包缺集、
  UTF-16 字幕和 ffsubsync 低质量结果。
- 继续收集真实任务中的失败模式，优先修复现有四个字幕源，不增加新 Provider。

## P2：对轴质量可解释性

- 调研并实现 ffsubsync 的 VAD 语音匹配率诊断输出，不把原始匹配分直接换算成百分比。
- 保持现有对轴分辨率不变；质量统计优先评估 100 ms 窗口与前后 200 ms 容差，并用真实正确、错误字幕样本校准。
- 仅在样本充分时展示语音匹配率；异常偏移、帧率修正和低质量拒绝仍作为独立信号。
- 第一阶段只提供透明指标，不设计复杂的综合置信度。

## 首个稳定版发布前

- 完成固定案例的 NAS 实机回归和容器重启验证。
- 完善安装、更新、回滚、备份和常见问题文档。
- 确认版本号、更新日志、许可证和多架构镜像均可用。

## 暂不计划

- 新增更多 Provider 或外部 Provider 市场
- 定时全库扫描
- Sonarr/Radarr 深度集成
- 多用户权限
- AI 自动选择、翻译或修改字幕
- 移动端专项适配
