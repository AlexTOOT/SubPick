# Roadmap

拾幕当前以“能稳定完成 MoviePilot 通知和 Jellyfin 手动补字幕”为目标，不扩展
成完整的 arr/Bazarr 替代品。

## P0：降低首次部署门槛

- 使用单一 `/appdata` 挂载，自动创建 `config.yaml`、`data` 和 `cache`。
- 提供可直接粘贴使用的 `compose.yaml`，只要求用户修改媒体目录映射。
- 移除首次部署对 `.env` 和手工创建目录、配置文件的依赖。
- 启动时检查配置目录、数据库和媒体目录的可读写状态。
- 当 MoviePilot 下发路径无法解析时，记录持久通知并在总览页提示处理。

## P1：首次使用与运行状态

- 增加可跳过、可继续的首次使用向导：
  - 媒体目录检查
  - MoviePilot API Key 生成与连接状态
  - Jellyfin 连接测试
  - Provider 配置与可用性测试
  - Zimuku OCR 实图测试
- 在总览页顶部显示按配置顺序排列的就绪进度；完成项为绿色，未完成项为红色，
  点击后跳转到对应设置，全部完成后自动隐藏。
- 增加“系统健康检查”，集中展示目录、数据库、外部工具、Jellyfin、Provider、OCR
  和最近一次 MoviePilot 回调状态。
- 增加动态通知区域；平时隐藏，仅在配置未完成、路径映射失败、Provider 不可用或
  其他需要用户处理的情况出现时显示。
- 支持配置导出、导入与基础备份。
- 为侧边栏使用统一的功能图标并提高文字可读性，保持现有拾幕配色和信息架构。
- 在任务详情中手动选择指定候选并执行下载、校验与落库。
- 提供整套镜像的更新检查和清晰的手动升级指引。
- 在 README 中增加拾幕 Logo 与经过统一裁切、脱敏和排版的真实界面预览。

## 发布

- GitHub Actions 自动运行代码检查、测试和 Docker 构建。
- 版本标签自动发布 `linux/amd64` 与 `linux/arm64` 镜像到 GHCR。
- 完善安装、更新、回滚、备份和常见问题文档。

在下一次修改匹配或校验规则前，建立小型离线回归样本，覆盖正确与错误年份、
电影与剧集串线、错误季集、整季字幕包、空文件、HTML 错误页、压缩包缺集和
ffsubsync 低质量结果。

## 暂不计划

- 新增更多 Provider
- 定时全库扫描
- Sonarr/Radarr 深度集成
- 多用户权限
- AI 自动选择、翻译或修改字幕
- 外部 Provider 插件市场
- 移动端专项适配
